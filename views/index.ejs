<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Journal</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-purple-100 font-sans antialiased">
    <div class="container mx-auto p-8">
        <!-- Main Container -->
        <div class="w-full max-w-3xl mx-auto bg-white rounded-lg shadow-xl p-8 space-y-6">
            
            <!-- Header -->
            <div class="text-center">
                <h1 class="text-4xl font-bold text-gray-800">My Journal🎀</h1>
                <p class="text-gray-600 mt-2">Write down your thoughts and reflect.</p>
            </div>

            <!-- Form Container -->
            <div class="bg-pink-100 rounded-lg p-6 shadow-md">
                <form action="/create" method="post" class="space-y-4">
                    <input 
                        type="text" 
                        class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-500" 
                        placeholder="What's on your mind?" 
                        name="title">
                    
                    <textarea 
                        class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-500 resize-none mt-3" 
                        placeholder="Relax and write..." 
                        name="details"></textarea>

                    <div class="flex justify-center">
                        <input 
                            class="mt-4 px-6 py-3 bg-pink-500 text-white font-semibold rounded-md cursor-pointer hover:bg-pink-600 transition duration-300" 
                            type="submit" value="Create Task">
                    </div>
                </form>
            </div>

            <!-- Task Display -->
            <div class="tasks grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <% if(files.length > 0){ %>
                    <% files.forEach(function(val){ %>
                        <% if(val !== ".gitkeep") { %>
                        <div class="task bg-pink-100 hover:bg-pink-200 transition duration-300 rounded-lg shadow-lg p-6">
                            <h1 class="text-xl font-semibold text-gray-800 mb-3"><%= val %></h1>
                            <div class="flex justify-between items-center text-sm text-gray-600">
                                <a href="/file/<%= val %>" class="hover:text-blue-500">Read more</a>
                                <a href="/edit/<%= val %>" class="hover:text-blue-500">Edit</a>
                            </div>
                        </div>
                    <% }}) %>
                <% } else { %>
                    <h3 class="text-center text-gray-500">No thoughts yet...</h3>
                <% } %>       
            </div>
        </div>
    </div>
</body>
</html>
