<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Thought</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-100 font-sans antialiased">
    <div class="container mx-auto p-8">
        <!-- Main Card -->
        <div class="w-full max-w-2xl mx-auto bg-white rounded-lg shadow-xl p-8 space-y-6">
            
            <!-- Back Button -->
            <div>
                <a class="text-blue-600 hover:underline text-base" href="/">← Back to Home</a>
            </div>

            <!-- Title -->
            <h1 class="text-3xl font-bold text-gray-800 text-center">Edit Your Thought</h1>
            <p class="text-center text-gray-500">Modify and update your existing entry below.</p>

            <!-- Edit Form -->
            <form action="/edit" method="post" class="space-y-5">
                <!-- Previous Filename -->
                <input 
                    type="text" 
                    name="prev" 
                    value="<%= filename %>" 
                    class="w-full px-5 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-400 bg-pink-100 text-gray-800 font-medium"
                    placeholder="Modify thought title">

                <!-- New Thought Name -->
                <input 
                    type="text"
                    name="new" 
                    placeholder="Relax and edit..." 
                    class="w-full px-5 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-400 bg-pink-100 text-gray-700">

                <!-- Submit Button -->
                <div class="flex justify-center">
                    <input 
                        type="submit" 
                        value="Update" 
                        class="px-6 py-3 bg-yellow-500 hover:bg-yellow-600 transition duration-300 text-white font-semibold rounded-md cursor-pointer">
                </div>
            </form>
        </div>
    </div>
</body>
</html>
