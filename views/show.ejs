<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Thought</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-pink-100 font-sans antialiased">
    <div class="container mx-auto p-8">
        <!-- Main Card -->
        <div class="w-full max-w-3xl mx-auto bg-white rounded-2xl shadow-2xl p-10 space-y-8 border border-pink-200">
            
            <!-- Back Button -->
            <div>
                <a class="text-pink-600 hover:underline text-base font-medium" href="/">← Back to Home</a>
            </div>

            <!-- Title -->
            <h1 class="text-4xl font-bold text-pink-700 break-words tracking-tight"><%= filename %></h1>

            <!-- Thought Content -->
            <p class="text-lg text-pink-900 whitespace-pre-line leading-relaxed tracking-wide font-light">
                <%= filedata %>
            </p>
        </div>
    </div>
</body>
</html>
